function gojax(type, url, data) {
	return $.ajax({
		url : url,
		type : type,
		cache : false,
		dataType : 'json',
		data : data
	});
}

function gojax_f(type, url, form_id) {
	return $.ajax({
		url : url,
		type : type,
		cache : false,
		dataType : 'json',
		data : $(form_id).serialize()
	});
}